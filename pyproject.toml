[project]
name = "relatorio-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Julio Trecenti", email = "julio.trecenti@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "plotnine>=0.15.4",
]

[project.scripts]
relatorio-demo = "relatorio_demo:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
